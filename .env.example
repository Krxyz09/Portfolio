# Frontend: base URL for backend API (no trailing slash)
VITE_API_BASE=https://your-backend.example.com

# Backend (optional) - if running backend locally or overriding default DB path
SQLITE_DB_PATH=./data/portfolio_contact_db.sqlite
PORT=3001
